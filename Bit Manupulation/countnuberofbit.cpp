#include <bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int l=0;
    int n;
    cin>>n;

    while(n>>l) l++;

    cout<<l;
}